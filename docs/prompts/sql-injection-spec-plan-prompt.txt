Read .claude/specs/sql-injection-fix.md (and re-read docs/TDD.md,
backend/app/services/auth_service.py, backend/app/core/security.py,
backend/pyproject.toml, and the root pyproject.toml).

Based on the spec, write a detailed, phase-by-phase implementation plan and
save it ONLY as .claude/specs/sql-injection-fix-plan.md.

Do NOT write or modify any source code in this step - produce the plan
document only. The plan must list, per phase: any DB driver/query-builder
dependency changes needed (if the existing library already supports
parameterized queries, state that no new dependency is required), the
exact changes to auth_service.py's signup()/login() query construction
(replacing string interpolation/concatenation with parameter binding/
prepared statements), confirmation that verify_password() from
security.py continues to be used unchanged for bcrypt comparison after
the user row is fetched, and the verification steps from the spec
(valid login, invalid login, injection payloads in username/password,
confirming 401 without SQL errors or crashes).

Do NOT change password hashing or authentication semantics beyond what
is required to remove the SQL injection. Do NOT modify
backend/app/core/security.py unless the read phase reveals the SQLi fix
genuinely requires it. Do NOT touch the /search route (VULN-3) or any
other vulnerability - this plan is strictly for VULN-1 (SQL Injection
in auth_service.py's signup()/login()). Preserve all other intentional
vulnerabilities untouched.

If .claude/specs/sql-injection-fix-plan.md already exists, do not
overwrite it silently - stop and report that it already exists instead
of writing the plan.

Output nothing else - no summary, no explanation, no restated sections,
no commentary before or after. Your entire response should be the file
creation action followed by a single line confirming the path.